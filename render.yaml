services:
  - type: web
    name: fastapi-text-similarity
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port 10000"
    envVars:
      - key: HF_TOKEN
        value: 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
